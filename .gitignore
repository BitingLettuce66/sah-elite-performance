# node_modules dir (main checkout) + the node_modules symlink wt.sh adds in worktrees
node_modules/
node_modules
dist/
dev-dist/
ds-bundle/
.DS_Store
*.log

# Secrets / local config
.env
.env.*
*.local

# Private context — kept on disk, never published
App-Spec.md
CLAUDE.local.md
.claude/settings.local.json

# Night-shift work log — kept private
NIGHT-SHIFT-LOG.md
